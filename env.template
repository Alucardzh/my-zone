# 复制为 .env 并按需修改

# ========================================
# 服务启用控制
# ========================================
# 数据库类型选择: postgres 或 sqlite
# 默认使用 postgres，如需使用 sqlite 请设置为 sqlite
# POSTGRES_DB_TYPE=sqlite

# 是否启用 SearXNG 搜索引擎服务（true 或 false）
# 如果需要搜索功能，可以设置为 true
ENABLE_SEARXNG=false

# ========================================
# Docker Compose 启动方式示例
# ========================================
# 使用 PostgreSQL + SearXNG: docker-compose --profile postgres --profile searxng up -d
# 使用 PostgreSQL 不含 SearXNG: docker-compose --profile postgres up -d
# 使用 SQLite + SearXNG: docker-compose --profile searxng up -d
# 使用 SQLite 不含 SearXNG: docker-compose up -d

# ========================================
# 数据库配置
# ========================================
# PostgreSQL 数据库配置（当 POSTGRES_DB_TYPE=postgres 或未设置时使用）
# 如果需要使用外部 postgres, 取消下方注释并修改下列数据库相关环境变量
# POSTGRES_EXTERNAL=true
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=my_navi
POSTGRES_USER=its_me
POSTGRES_PASSWORD=itsmynavidb

# SQLite 数据库配置（当 POSTGRES_DB_TYPE=sqlite 时使用）
# POSTGRES_SQLITE_DB_PATH=/app/db_data/my_navi.sqlite3

# ========================================
# SUPERADMIN 配置
# ========================================

# SUPERADMIN
# SUPERADMIN_NAME=admin
# SUPERADMIN_PASSWORD=NmAFqAuCG6T6z1

# JWT 配置（请在生产环境修改为强随机值）
JWT_SECRET=dev-secret-change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ========================================
# SearXNG 搜索引擎配置
# ========================================

# Searxng
SEARXNG_BASE_URL=
SEARXNG_BIND_HOST=0.0.0.0
SEARXNG_BIND_PORT=58080

# ========================================
# 对外端口
# ========================================

# Web Port
WEB_PORT=8380
